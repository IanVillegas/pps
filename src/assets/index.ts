// Barril de assets propios de DecPat. Sigue la convencion de
// src/sad-aml-shared/assets: imagenes en ./images, importadas directo
// (Next/webpack las resuelve como modulo, no como archivo estatico de public/).
//
// Assets 2026 pendientes de exportar desde Figma (ver README.md de esta
// carpeta para el detalle de cada uno y su nodo de origen). No se agregan
// exports aqui hasta que el archivo real este en ./images: un export
// apuntando a un archivo inexistente rompe el build en vez de documentar
// el pendiente.
export {};
